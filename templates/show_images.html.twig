{% extends 'base.html.twig' %}

{% block title %}Hello ShowImagesController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }

        img {
            max-width: 200px;
            height: auto;
        }
    </style>

    <div class="example-wrapper">
        <p><a href="{{ path('app_index') }}">К форме ввода URL</a></p>
        {% set column = 0 %}
        <table>
            {% for image in images %}
                {% if column == 0 %}
                    <tr>
                {% endif %}
                <td>
                    <img src={{ image }}>
                </td>
                {% set column = column + 1 %}
                {% if column == 4 %}
                    </tr>
                    {% set column = 0 %}
                {% endif %}
            {% endfor %}
        </table>
        <p>
            На странице обнаружено {{ images|length }} изображений общим объёмом {{ (imagesSize / 1024 / 1024)|number_format(2) }} Мб
        </p>
    </div>
{% endblock %}
